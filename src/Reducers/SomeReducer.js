const initialState = {
    title: 'Select component to show'
};

export default function SomeReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};